{
  "name": "nests-monitor",
  "version": "1.0.0",
  "description": "NESTS notice monitor using Cloudflare Workers and D1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail"
  },
  "dependencies": {
    "cheerio": "^1.1.2"
  },
  "devDependencies": {
    "wrangler": "^4.35.0"
  }
}
